on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: "git clone https://github.com/fabian-hiller/valibot.git valibotdir"
      - run: "git -C valibotdir log -n 5"
      - run: "git log -n 5"
